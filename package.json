{
  "name": "lfn-sftp-client",
  "version": "1.0.0",
  "description": "Lineage SFTP client for file transfer.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "brandon.hedge@lineagebank.com",
  "license": "UNLICENSED",
  "dependencies": {
    "@slack/webhook": "^6.0.0",
    "dotenv": "^10.0.0",
    "moment": "^2.29.1",
    "ms-teams-webhook": "^1.0.4",
    "mv": "^2.1.1",
    "ssh2-sftp-client": "^7.2.0",
    "winston": "^3.3.3"
  }
}
